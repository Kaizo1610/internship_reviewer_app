import 'package:flutter/material.dart';

class AddPosting extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Posting'),
      ),
      body: Center(
        child: Text('Add your posting details here'),
      ),
    );
  }
}